defmodule Exoforge.Core do
  use Mix.Project

  def project do
    [
      app: :exoforge_core,
      version: "0.1.0",
      elixir: "~> 1.20",
      deps: deps()
    ]
  end

  defp deps do
    []
  end
end
